const apiPath = 'http://localhost:8080/api';

export default {
  // POST
//   createProject: () => [apiPath, 'project'].join('/'),

  //GET
  allCategories: () => [apiPath, 'categories'].join('/'),

  // PATCH
//   updateProject: (id) => [apiPath, 'project', id].join('/'),

  // DELETE
//   deleteProject: (id) => [apiPath, 'project', id].join('/'),

};